import React, { FC } from 'react';

import Routing from './components/Routing/Routing';

const App: FC = () => {
    return (
        <>
            <Routing />
        </>
    );
};

export default App;
