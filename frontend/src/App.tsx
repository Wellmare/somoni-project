import React, { FC } from 'react';

import Router from './components/routing/Router';

const App: FC = () => {
    return (
        <>
            <Router />
        </>
    );
};

export default App;
