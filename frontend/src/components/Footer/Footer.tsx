import classNames from 'classnames';
import React, { FC } from 'react';

const Footer: FC = () => {
    return <footer className={classNames('p-5', 'w-full', 'mt-3', 'bg-gray-600')}>footer</footer>;
};

export default Footer;
